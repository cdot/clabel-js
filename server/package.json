{
    "name": "clabel-js",
    "version": "0.0.1",
    "type": "module",
    "description": "CLabel",
    "main": "main.js",
    "scripts": {
		"test": "mocha --recursive --extension js --timeout=5000 test"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.21.1",
        "posix-getopt": "^1.2.1",
        "sharp": "^0.33.5"
    },
    "devDependencies": {
        "assert": "^2.1.0",
        "chai": "^5.1.1",
        "mocha": "^10.7.3",
        "tmp-promise": "^3.0.3"
    },
  "scripts": {
	"lint": "eslint src -f unix",
	"test": "mocha --recursive --extension js --timeout=5000 test"
  }
}
